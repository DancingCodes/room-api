# Room API Project Memory

## Current Status

- Backend first version is implemented and usable for Android integration.
- Server listens on fixed port `8080`.
- Ordinary HTTP APIs use status `200` with body shape: `code`, `message`, `data`; success message is `成功`, common validation failure message is `参数错误`, auth failure message is `未登录`. WebSocket auth failure returns real HTTP `401` during the handshake.
- CORS accepts all origins.
- Database schema is managed manually with `migrations/001_init_schema.sql`; do not use AutoMigrate.
- `.env` is local only and must not be committed or printed.
- `.idea/` is ignored and should not be committed.
- Line endings: keep source, docs, SQL and config files as LF; keep Windows scripts such as `.ps1` as CRLF. Do not add `.gitattributes` unless explicitly requested.

## Implemented Features

- Auth: email verification code send and email verification code login.
- `POST /api/v1/auth/email-code` sends a code for the email.
- `POST /api/v1/auth/email-login` verifies the code; if the email is new, the backend automatically creates the user and returns `token + user`.
- New users get an auto-generated nickname from the email local part and a deterministic generated avatar URL.
- Account, password, standalone register, and password reset are no longer used.
- User: current profile and profile update with both nickname and avatar URL.
- Email: Tencent SES template sending with `TemplateData` containing `code`.
- Rooms: list, create, detail, join, leave, owner transfer, mic status.
- Messages: text message create/list.
- WebSocket: `/api/v1/ws/rooms/:room_id` using `Authorization: Bearer jwt_token`.
- Agora RTC Token: `GET /api/v1/rooms/:room_id/rtc-token` verifies room membership and issues a 1-hour Token for `room-{roomId}` and the current user uid. `AGORA_APP_CERTIFICATE` stays only in the server environment.
- Android client project is the sibling directory `../room-app`.
- App version endpoint: `GET /api/v1/app-version/latest` returns the largest published `version_code`; the Android client treats a successful response without a newer version as the prerequisite for session restoration. Endpoint failures block client entry until retry.

## Important Decisions

- Users authenticate by email verification code only.
- The Android auth flow uses only `email-code` and `email-login`.
- New user creation happens inside `email-login`; the client does not call a separate register endpoint.
- Generated nickname is based on the email local part, capped at 8 Unicode characters, with numeric suffixes added on conflict.
- Generated avatar URL uses a stable email seed; clients can upload an image through `/api/v1/uploads/image`, then save both `nickname` and `avatar_url` through `PATCH /api/v1/users/me`.
- JWT valid duration is 100 calendar years.
- WebSocket auth uses request header, not query string.
- One user can be in only one room at a time.
- Room name is `用户昵称 + " 的房间"`.
- Last member leaving deletes the room and messages.
- Owner leaving transfers ownership to the earliest joined remaining member.
- Email verification code TTL is 5 minutes.
- Email code send limits: 60 seconds cooldown, 5 per hour per email/purpose.
- Tencent SES and COS share `TENCENT_SECRET_ID` and `TENCENT_SECRET_KEY`.
- Production DB and test DB are separate. `ROOM_TEST_MYSQL_DSN` points to `room_test`.

## Key Files

- Entry: `cmd/api/main.go`
- Router: `internal/router/router.go`
- Handlers: `internal/handler`
- Services: `internal/service`
- Repositories: `internal/repository`
- Models: `internal/model`
- Realtime hub: `internal/realtime/hub.go`
- Migration: `migrations/001_init_schema.sql`
- API docs: `接口文档.md`
- Backend design: `后端方案.md`
- Smoke scripts: `scripts`

## Common Commands

Windows / PowerShell:

```powershell
D:\DancingCodes\goSdk\go1.26.0\bin\go.exe test ./...
```

Request a code, then run smoke scripts with the received code:

```powershell
Invoke-RestMethod -Method POST -Uri "http://127.0.0.1:8080/api/v1/auth/email-code" -ContentType "application/json" -Body '{"email":"your@email.com"}'
.\scripts\api-smoke.ps1 -Email "your@email.com" -EmailCode "123456"
.\scripts\ws-smoke.ps1 -Email "your@email.com" -EmailCode "123456"
```

Real email flow:

```powershell
.\scripts\email-smoke.ps1 -Email "your@email.com"
```

## Verified

- Non-repository packages compile and pass tests.
- Repository integration tests require `room_test` to exist and have current migrations applied.

## Next Step

Continue Android/backend integration from `../room-app` and keep API behavior aligned with `docs/接口文档.md`.