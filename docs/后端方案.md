# Room 后端方案

## 目标

主流程：

```txt
邮箱验证码注册/登录 -> 房间列表 -> 创建或进入房间 -> 房间详情
```

## 测试

普通测试：

```powershell
D:\DancingCodes\goSdk\go1.26.0\bin\go.exe test ./...
```

数据库集成测试需要准备专用测试库并执行当前 migrations。`ROOM_TEST_MYSQL_DSN` 指向未初始化数据库时，仓储集成测试会失败。

```powershell
$env:ROOM_TEST_MYSQL_DSN="user:password@tcp(host:3306)/room_test?charset=utf8mb4&parseTime=True&loc=Local"
D:\DancingCodes\goSdk\go1.26.0\bin\go.exe test ./internal/repository -v
```

本地接口冒烟测试：

```powershell
.\scripts\email-smoke.ps1 -Email "your@email.com"
.\scripts\api-smoke.ps1 -Email "your@email.com" -EmailCode "123456"
.\scripts\ws-smoke.ps1 -Email "your@email.com" -EmailCode "123456"
```

## 第一版后端提供

- 邮箱验证码注册
- 邮箱验证码登录
- 当前用户资料
- 头像上传
- 房间创建、列表、详情、进入、离开
- 房主自动转让
- 文字消息
- WebSocket 房间事件广播
- 开麦/闭麦状态同步

## 技术栈

- Go
- Gin
- GORM
- MySQL
- JWT
- WebSocket
- 腾讯云 SES
- 腾讯云 COS
- SQL migration

## 环境变量

```txt
JWT_SECRET=change_me
MYSQL_DSN=room:password@tcp(127.0.0.1:3306)/room?charset=utf8mb4&parseTime=True&loc=Asia%2FShanghai

TENCENT_SECRET_ID=your_tencent_cloud_secret_id
TENCENT_SECRET_KEY=your_tencent_cloud_secret_key
TENCENT_SES_REGION=ap-hongkong
TENCENT_SES_FROM=Room <moonc@ses.moonc.love>
TENCENT_SES_TEMPLATE_ID=186671

COS_BASE_URL=https://your_bucket.cos.ap-guangzhou.myqcloud.com
COS_PATH_PREFIX=room
COS_CDN_URL=https://your_cdn_domain
```

服务端口固定为 `8080`。CORS 接受全部来源。

数据库表结构使用 SQL migration 管理。新库执行 `migrations/001_init_schema.sql`；旧库从账号密码模型升级到邮箱验证码模型时，备份后执行 `migrations/002_email_auth.sql`。

服务启动时只连接 MySQL，不自动创建、不修改、不执行 migration。数据库连接失败时，服务启动失败并退出。

## 数据表

### users

```sql
CREATE TABLE users (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  email VARCHAR(128) NOT NULL,
  nickname VARCHAR(8) NOT NULL,
  avatar_url VARCHAR(512) NOT NULL,
  created_at DATETIME NOT NULL,
  updated_at DATETIME NOT NULL,
  UNIQUE KEY uk_users_email (email),
  UNIQUE KEY uk_users_nickname (nickname)
);
```

### email_verification_codes

```sql
CREATE TABLE email_verification_codes (
  id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT UNSIGNED NULL,
  email VARCHAR(128) NOT NULL,
  purpose VARCHAR(32) NOT NULL,
  code VARCHAR(16) NOT NULL,
  used_at DATETIME NULL,
  expires_at DATETIME NOT NULL,
  created_at DATETIME NOT NULL,
  KEY idx_email_codes_email_purpose_created_at (email, purpose, created_at)
);
```

规则：

- `purpose` 为 `register` 或 `login`。
- 验证码 5 分钟有效。
- 同一邮箱同一用途 60 秒内只能发送一次。
- 同一邮箱同一用途 1 小时最多发送 5 次。
- 同一邮箱同一用途重发后，旧验证码失效。

### rooms / room_members / messages

保持现有房间、成员、消息表结构。`room_members.uk_room_members_user` 保证一个用户同一时间只能在一个房间。房主只以 `rooms.owner_id` 为准，成员 `is_owner` 由接口计算。

## 业务规则

通用：

- 时间统一返回 ISO 8601 格式，例如 `2026-06-16T16:00:00+08:00`。
- HTTP 响应统一包含 `code`、`message`、`data`。
- 字符长度按 Unicode 字符数计算。
- 字符串字段 trim 后再校验和入库。
- 邮箱 trim 后转小写。
- 密钥只通过环境变量配置，不提交到仓库。

用户：

- 用户不再有账号和密码字段。
- 邮箱唯一，用邮箱验证码注册和登录。
- 昵称 trim 后 1-8 个字符且唯一。
- 头像必填。
- JWT 有效期 100 日历年。
- JWT 签名算法使用 `HS256`。
- JWT claims 至少包含 `user_id` 和 `exp`。
- `current_room_id` 不在 `users` 表存储，接口返回时从 `room_members` 查询。

验证码：

- 验证码为 6 位数字。
- `POST /api/v1/auth/register-code` 只给未注册邮箱发送注册验证码。
- `POST /api/v1/auth/login-code` 给已注册邮箱发送登录验证码；邮箱不存在时仍返回成功，避免暴露注册状态。
- 校验验证码时，只接受未使用、未过期的最新一条同用途验证码。
- 验证码邮件使用腾讯云 SES API 发送，`TemplateData` 传 `{"code":"123456"}`。

房间、消息、WebSocket 规则保持不变：一个用户同一时间只能在一个房间；WebSocket 断开等于离开房间；HTTP 发送消息后广播 `message.created`；房主离开后自动转让或删除空房间。