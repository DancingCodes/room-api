# Room 接口文档

## 通用规则

接口前缀：

```txt
/api/v1
```

HTTP 响应统一格式：

```json
{
  "code": 200,
  "message": "ok",
  "data": {}
}
```

业务 code：

- `200` 成功
- `401` 鉴权失败
- `500` 错误

HTTP 状态码统一返回 `200`，WebSocket 鉴权失败返回 HTTP `401`。

失败响应：

```json
{
  "code": 500,
  "message": "error message",
  "data": null
}
```

常用错误 message：

- `unauthorized`
- `invalid params`
- `username already exists`
- `email already exists`
- `nickname already exists`
- `invalid email code`
- `username or password is incorrect`
- `avatar is required`
- `room not found`
- `room is full`
- `user already in room`
- `not room member`
- `message content is invalid`
- `invalid mic status`
- `email send failed`
- `upload failed`

时间统一返回 ISO 8601 格式：

```txt
2026-06-16T16:00:00+08:00
```

鉴权请求头：

```txt
Authorization: Bearer jwt_token
```

参数规则：

- 字符长度按 Unicode 字符数计算
- 邮箱 trim 后转小写
- 用户名正则为 `^[A-Za-z0-9_]{4,20}$`
- 昵称 trim 后 1-8 个字符
- 密码 6-20 位，密码不 trim
- 消息内容 trim 后 1-50 个字符

免登录接口：

- `POST /api/v1/uploads/avatar`
- `POST /api/v1/auth/register-code`
- `POST /api/v1/auth/register`
- `POST /api/v1/auth/login`
- `POST /api/v1/auth/password-reset-code`
- `POST /api/v1/auth/reset-password`

## 通用对象

用户：

```json
{
  "id": 1,
  "username": "alex_001",
  "email": "alex@example.com",
  "nickname": "Alex",
  "avatar_url": "https://example.com/room/uuid.png",
  "current_room_id": 1,
  "created_at": "2026-06-16T16:00:00+08:00",
  "updated_at": "2026-06-16T16:00:00+08:00"
}
```

`current_room_id` 表示当前用户所在房间 ID，没有所在房间时为 `null`。

房间：

```json
{
  "id": 1,
  "name": "Alex 的房间",
  "owner_id": 1,
  "current_members": 1,
  "max_members": 8,
  "created_at": "2026-06-16T16:00:00+08:00"
}
```

消息里的 `sender_nickname` 和 `sender_avatar_url` 使用发送者当前用户资料。

房间成员：

```json
{
  "user_id": 1,
  "nickname": "Alex",
  "avatar_url": "https://example.com/room/uuid.png",
  "is_owner": true,
  "mic_status": "off",
  "joined_at": "2026-06-16T16:00:00+08:00"
}
```

消息：

```json
{
  "id": 10,
  "room_id": 1,
  "sender_id": 1,
  "sender_nickname": "Alex",
  "sender_avatar_url": "https://example.com/room/uuid.png",
  "type": "text",
  "content": "hello",
  "created_at": "2026-06-16T16:00:00+08:00"
}
```

## 健康检查

```txt
GET /health
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "status": "up"
  }
}
```

## 认证

### 注册前上传头像

```txt
POST /api/v1/uploads/avatar
```

`multipart/form-data`：

```txt
file: 头像文件
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "avatar_url": "https://example.com/room/uuid.png"
  }
}
```

### 发送注册验证码

```txt
POST /api/v1/auth/register-code
```

请求：

```json
{
  "email": "alex@example.com"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": null
}
```

### 注册

```txt
POST /api/v1/auth/register
```

请求：

```json
{
  "username": "alex_001",
  "email": "alex@example.com",
  "email_code": "123456",
  "password": "123456",
  "nickname": "Alex",
  "avatar_url": "https://example.com/room/uuid.png"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "token": "jwt_token",
    "user": {
      "id": 1,
      "username": "alex_001",
      "email": "alex@example.com",
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "current_room_id": null,
      "created_at": "2026-06-16T16:00:00+08:00",
      "updated_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

### 登录

```txt
POST /api/v1/auth/login
```

请求：

```json
{
  "username": "alex_001",
  "password": "123456"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "token": "jwt_token",
    "user": {
      "id": 1,
      "username": "alex_001",
      "email": "alex@example.com",
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "current_room_id": null,
      "created_at": "2026-06-16T16:00:00+08:00",
      "updated_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

### 发送重置密码验证码

```txt
POST /api/v1/auth/password-reset-code
```

请求：

```json
{
  "email": "alex@example.com"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": null
}
```

### 重置密码

```txt
POST /api/v1/auth/reset-password
```

请求：

```json
{
  "email": "alex@example.com",
  "code": "123456",
  "new_password": "new123456"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": null
}
```

## 用户

### 当前用户

```txt
GET /api/v1/users/me
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "user": {
      "id": 1,
      "username": "alex_001",
      "email": "alex@example.com",
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "current_room_id": null,
      "created_at": "2026-06-16T16:00:00+08:00",
      "updated_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

### 更新当前用户

```txt
PATCH /api/v1/users/me
```

请求：

```json
{
  "nickname": "Alex"
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "user": {
      "id": 1,
      "username": "alex_001",
      "email": "alex@example.com",
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "current_room_id": null,
      "created_at": "2026-06-16T16:00:00+08:00",
      "updated_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

### 更新头像

```txt
POST /api/v1/users/me/avatar
```

`multipart/form-data`：

```txt
file: 头像文件
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "user": {
      "id": 1,
      "username": "alex_001",
      "email": "alex@example.com",
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/new-avatar.png",
      "current_room_id": null,
      "created_at": "2026-06-16T16:00:00+08:00",
      "updated_at": "2026-06-16T16:10:00+08:00"
    }
  }
}
```

## 房间

### 创建房间

```txt
POST /api/v1/rooms
```

创建成功后用户已经在房间内，前端随后建立 WebSocket。

请求：

```json
{
  "max_members": 8
}
```

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "room": {
      "id": 1,
      "name": "Alex 的房间",
      "owner_id": 1,
      "current_members": 1,
      "max_members": 8,
      "created_at": "2026-06-16T16:00:00+08:00"
    },
    "members": [
      {
        "user_id": 1,
        "nickname": "Alex",
        "avatar_url": "https://example.com/room/uuid.png",
        "is_owner": true,
        "mic_status": "off",
        "joined_at": "2026-06-16T16:00:00+08:00"
      }
    ]
  }
}
```

### 房间列表

```txt
GET /api/v1/rooms?page=1&page_size=20
```

分页：

- 默认 `page = 1`
- 默认 `page_size = 20`
- 最大 `page_size = 50`

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "Alex 的房间",
        "owner_id": 1,
        "current_members": 1,
        "max_members": 8,
        "created_at": "2026-06-16T16:00:00+08:00"
      }
    ],
    "total": 1,
    "page": 1,
    "page_size": 20
  }
}
```

空列表响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "list": [],
    "total": 0,
    "page": 1,
    "page_size": 20
  }
}
```

### 房间详情

```txt
GET /api/v1/rooms/:room_id
```

要求当前用户已在该房间内。

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "room": {
      "id": 1,
      "name": "Alex 的房间",
      "owner_id": 1,
      "current_members": 1,
      "max_members": 8,
      "created_at": "2026-06-16T16:00:00+08:00"
    },
    "members": [
      {
        "user_id": 1,
        "nickname": "Alex",
        "avatar_url": "https://example.com/room/uuid.png",
        "is_owner": true,
        "mic_status": "off",
        "joined_at": "2026-06-16T16:00:00+08:00"
      }
    ]
  }
}
```

### 进入房间

```txt
POST /api/v1/rooms/:room_id/join
```

进入成功后用户已经在房间内，前端随后建立 WebSocket。

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "room": {
      "id": 1,
      "name": "Alex 的房间",
      "owner_id": 1,
      "current_members": 2,
      "max_members": 8,
      "created_at": "2026-06-16T16:00:00+08:00"
    },
    "members": [
      {
        "user_id": 1,
        "nickname": "Alex",
        "avatar_url": "https://example.com/room/uuid.png",
        "is_owner": true,
        "mic_status": "off",
        "joined_at": "2026-06-16T16:00:00+08:00"
      },
      {
        "user_id": 2,
        "nickname": "Mia",
        "avatar_url": "https://example.com/room/mia.png",
        "is_owner": false,
        "mic_status": "off",
        "joined_at": "2026-06-16T16:01:00+08:00"
      }
    ]
  }
}
```

空列表响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "list": [],
    "limit": 20
  }
}
```

### 离开房间

```txt
POST /api/v1/rooms/:room_id/leave
```

离开接口幂等，用户不在房间内也返回成功。

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": null
}
```

### 更新麦克风状态

```txt
PATCH /api/v1/rooms/:room_id/mic
```

请求：

```json
{
  "mic_status": "on"
}
```

`mic_status` 可选：

- `on`
- `off`

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "member": {
      "user_id": 1,
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "is_owner": true,
      "mic_status": "on",
      "joined_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

## 消息

### 历史消息

```txt
GET /api/v1/rooms/:room_id/messages?limit=20&before_id=100
```

要求当前用户已在该房间内。

分页：

- 默认 `limit = 20`
- 最大 `limit = 50`
- 传入 `before_id` 时，返回 `id < before_id` 的消息
- 查询时按 `id` 倒序分页，返回时按 `id` 正序

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "list": [
      {
        "id": 10,
        "room_id": 1,
        "sender_id": 1,
        "sender_nickname": "Alex",
        "sender_avatar_url": "https://example.com/room/uuid.png",
        "type": "text",
        "content": "hello",
        "created_at": "2026-06-16T16:00:00+08:00"
      }
    ],
    "limit": 20
  }
}
```

### 发送文字消息

```txt
POST /api/v1/rooms/:room_id/messages
```

请求：

```json
{
  "content": "hello"
}
```

规则：

- 最多 50 字
- 后端 trim
- trim 后为空则拒绝
- 要求当前用户已在该房间内

响应：

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "message": {
      "id": 10,
      "room_id": 1,
      "sender_id": 1,
      "sender_nickname": "Alex",
      "sender_avatar_url": "https://example.com/room/uuid.png",
      "type": "text",
      "content": "hello",
      "created_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

## WebSocket

```txt
GET /ws/v1/rooms/:room_id?token=jwt_token
```

规则：

- 心跳使用 WebSocket ping/pong
- 服务端检测心跳超时后关闭连接，并执行离开逻辑
- `member.joined` 广播给加入成功时房间内已有 WebSocket 连接
- `message.created` 广播给房间内所有 WebSocket 连接，包括发送者
- 主动离开和 WebSocket 断开共用同一套离开逻辑
- 离开逻辑幂等，只有实际删除成员记录的那一次广播 `member.left`
- 房主离开且房间仍有成员时，先广播 `member.left`，再广播 `room.owner_changed`
- `room.owner_changed.data.members` 返回房间内所有剩余成员

### member.joined

```json
{
  "type": "member.joined",
  "room_id": 1,
  "data": {
    "member": {
      "user_id": 2,
      "nickname": "Mia",
      "avatar_url": "https://example.com/room/mia.png",
      "is_owner": false,
      "mic_status": "off",
      "joined_at": "2026-06-16T16:01:00+08:00"
    },
    "current_members": 2
  }
}
```

### member.left

```json
{
  "type": "member.left",
  "room_id": 1,
  "data": {
    "user_id": 2,
    "current_members": 1
  }
}
```

### room.owner_changed

```json
{
  "type": "room.owner_changed",
  "room_id": 1,
  "data": {
    "owner_id": 2,
    "members": [
      {
        "user_id": 2,
        "nickname": "Mia",
        "avatar_url": "https://example.com/room/mia.png",
        "is_owner": true,
        "mic_status": "off",
        "joined_at": "2026-06-16T16:01:00+08:00"
      }
    ]
  }
}
```

### message.created

```json
{
  "type": "message.created",
  "room_id": 1,
  "data": {
    "message": {
      "id": 10,
      "room_id": 1,
      "sender_id": 1,
      "sender_nickname": "Alex",
      "sender_avatar_url": "https://example.com/room/uuid.png",
      "type": "text",
      "content": "hello",
      "created_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```

### member.mic_updated

```json
{
  "type": "member.mic_updated",
  "room_id": 1,
  "data": {
    "member": {
      "user_id": 1,
      "nickname": "Alex",
      "avatar_url": "https://example.com/room/uuid.png",
      "is_owner": true,
      "mic_status": "on",
      "joined_at": "2026-06-16T16:00:00+08:00"
    }
  }
}
```
